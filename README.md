# Setup

To install the bioconductor package:
```
install.packages("BiocManager")
BiocManager::install(version = "3.22")
BiocManager::install(c("ggrepel", "scater", "airpart"))
```

See https://www.bioconductor.org/install/
